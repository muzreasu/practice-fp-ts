const add1 = (num: number): number => num + 1;

const multiply24 = (num: number): number => num * 24;

const addAndMultiplePipe = () => {
  // Implement given we have a number we need to add 1 to this number then multiple the result with 24 with the pipe functions.
}

const addAndMultipleFlow = () => {
  // Implement given we have a number we need to add 1 to this number then multiple the result with 24 with the flow functions.
}